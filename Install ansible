# install ansible
On Ubuntu/Debian/Linux Mint
 sudo apt-get install ansible
On RHEL/CentOS/Fedora
 sudo yum install epel-release

 sudo yum install ansible
 
 # Check version
 ansible --version
 
 # Run playbooks
 ansible-playbook -i myhosts file_name.yaml
